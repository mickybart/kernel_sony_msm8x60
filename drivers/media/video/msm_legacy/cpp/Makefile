GCC_VERSION      := $(shell $(CONFIG_SHELL) $(PWD)/scripts/gcc-version.sh $(CROSS_COMPILE)gcc)
ccflags-y += -Idrivers/media/video/msm_legacy
ccflags-y += -Idrivers/media/video/msm_legacy/io
obj-$(CONFIG_MSM_CPP) += msm_cpp.o

